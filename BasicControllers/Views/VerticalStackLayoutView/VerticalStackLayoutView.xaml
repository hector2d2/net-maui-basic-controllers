<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BasicControllers.Views.VerticalStackLayoutView.VerticalStackLayoutView"
             x:Name="Vlayout">
    <ScrollView>
        <VerticalStackLayout HorizontalOptions="Center">
            <Label Text="Hola mundo" />
            <Label Text="Hola mundo" />
            <Label Text="Hola mundo" />
            <VerticalStackLayout BindableLayout.ItemsSource="{Binding Numeros}"
                                BindingContext="{x:Reference Vlayout}">
                <BindableLayout.ItemTemplate>
                    <DataTemplate>
                        <Label Text="{Binding .}"/>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>